name: CI

on:
  pull_request:
    branches: [Dev, master]
  push:
    branches: [Dev, master]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  frontend:
    name: Frontend
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.19.0
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

  backend:
    name: Backend
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.19.0
          cache: npm
          cache-dependency-path: |
            backend/package-lock.json
            services/lobby/package-lock.json
            services/roles/package-lock.json

      - name: Install gateway dependencies
        working-directory: backend
        run: npm ci

      - name: Install Lobby Service dependencies
        working-directory: services/lobby
        run: npm ci

      - name: Install Role Service dependencies
        working-directory: services/roles
        run: npm ci

      - name: Check server modules
        run: |
          node --check backend/src/server.js
          node --check services/lobby/src/server.js
          node --check services/lobby/src/app.js
          node --check services/roles/src/server.js
          node --check services/roles/src/app.js

      - name: Test role balancing and win conditions
        working-directory: services/roles
        run: npm test

      - name: Start backend services
        run: docker compose up --build --detach

      - name: Verify health endpoints
        run: |
          for attempt in {1..60}; do
            if curl --fail --silent --show-error http://127.0.0.1:3001/health && \
               curl --fail --silent --show-error http://127.0.0.1:3002/health && \
               curl --fail --silent --show-error http://127.0.0.1:3003/health; then
              echo "Tất cả các service đã sẵn sàng!"
              exit 0
            fi
            echo "Đang đợi các service khởi động (Lần thử $attempt)..."
            sleep 2
          done

          echo "Khởi động service thất bại. In log:"
          docker compose logs
          exit 1

      - name: Test multiplayer integration flow
        working-directory: backend
        run: npm run test:integration

      - name: Stop backend services
        if: always()
        run: docker compose down