export const ROLES = Object.freeze({
  VILLAGER: "villager",
  WEREWOLF: "werewolf",
  SEER: "seer",
  DOCTOR: "doctor"
});

export const DEFAULT_ROLE_SET = Object.freeze([
  ROLES.VILLAGER,
  ROLES.WEREWOLF,
  ROLES.SEER,
  ROLES.DOCTOR
]);
